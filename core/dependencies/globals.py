import os

from core.utils.logger import Logger
from core.utils import  config

logger = Logger("audio_supporter")
configer = config.get_config()

# 获取项目根目录的绝对路径




if __name__ == "__main__":
    print(configer)